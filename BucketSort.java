import java.util.ArrayList;
import java.util.List;

public class BucketSort {

    // public static void buckSortArray(int[] arr, int n) {
    //     if(n < 1) return;                               // Bucket size should be greater than 0 
    
    //     Vec buckets = new int[n];                     // Creating the bucket
    
        
    // }

    public static void buckSortList(List<Integer> lst, int n) {
        if(n < 1) return;                               // Bucket size should be greater than 0
    
        List<Integer> bucket = new ArrayList<>();       // Creating bucket
    
    }


    public static void main(String[] args) {
        System.out.println(9/5);
    }
}
